<div class="container px-4 mt-4">
<h3 class="mt-0 mb-4">My Mentor</h3>
<div class="card-small card-small-green">
  <%= link_to "picture of user", connection_path(@connection) %>
  <div class="card-small-infos">
  <%# to do: merge info %>
    <% if @connection %>
        <h2><%= @connection.mentor.user.first_name %></h2>
      <% else %>
        <h2><%= link_to 'choose your mentor', mentors_path %></h2>
      <% end %>
    <p>💬 🏆</p>
  </div>`
    <h2>user name</h2>
    <%= link_to chatroom_path(Chatroom.last) do %>
      <i class="fa-regular fa-message fa-3x"></i>
    <% end %>
    <%= link_to connection_tasks_path(@connection) do %>
      <i class="fa-solid fa-timeline"></i>
    <% end %>
    <p> 🏆</p>
  </div>
</div>

<br>

<h3 class="mt-0 mb-4"> Previous Mentors</h3>

    <div class="card-small-gray">
      <%= link_to "picture of user", "#"%>
        <div class="card-small-infos">
          <h2>user name</h2>
          <p>💬   🏆</p>
        </div>
    </div>

    <div class="card-small-gray">
      <%= link_to "picture of user", "#"%>
        <div class="card-small-infos">
          <h2>user name</h2>
          <p>💬   🏆</p>
        </div>
    </div>
      <div class="card-small-gray">
      <%= link_to "picture of user", "#"%>
        <div class="card-small-infos">
          <h2>user name</h2>
          <p>💬   🏆</p>
        </div>
    </div>

  </div>
