<div class="container-xl px-4 mt-4">
  <h2>My mentor</h2>
  <hr class="mt-0 mb-4">

<% if current_user.role == "mentor" %>
  <%= render 'mentors/show_mentee' %>
<% else %>
  <%= render 'mentees/show_mentor', locals: {mentor: @mentor} %>
<% end %>
